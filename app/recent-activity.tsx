"use client";
import{useEffect,useMemo,useState}from"react";import{supabase}from"../lib/supabase";
type Activity={id:string;title:string;type:string;detail:string;updated_at:string;href:string};
export function RecentActivity({userId}:{userId:string}){const[items,setItems]=useState<Activity[]>([]),[filter,setFilter]=useState("All"),[expanded,setExpanded]=useState(false);useEffect(()=>{Promise.all([supabase.from("college_applications").select("id,college_name,status,updated_at").eq("user_id",userId).order("updated_at",{ascending:false}).limit(10),supabase.from("scholarship_applications").select("id,scholarship_name,status,updated_at").eq("user_id",userId).order("updated_at",{ascending:false}).limit(10),supabase.from("essay_drafts").select("id,title,stage,updated_at").eq("user_id",userId).order("updated_at",{ascending:false}).limit(10),supabase.from("resume_versions").select("id,title,updated_at").eq("user_id",userId).order("updated_at",{ascending:false}).limit(10),supabase.from("student_steps").select("id,title,category,completed,updated_at").eq("user_id",userId).order("updated_at",{ascending:false}).limit(10),supabase.from("student_support_contacts").select("id,name,role,updated_at").eq("user_id",userId).order("updated_at",{ascending:false}).limit(10),supabase.from("student_college_decisions").select("id,school_name,updated_at").eq("user_id",userId).order("updated_at",{ascending:false}).limit(10),supabase.from("practice_test_logs").select("id,exam,total_score,created_at").eq("user_id",userId).order("created_at",{ascending:false}).limit(10)]).then(([apps,awards,essays,resumes,steps,contacts,decisions,tests])=>{const all:Activity[]=[...(apps.data??[]).map(x=>({id:`app-${x.id}`,title:x.college_name,type:"Applications",detail:x.status,updated_at:x.updated_at,href:"#applications"})),...(awards.data??[]).map(x=>({id:`award-${x.id}`,title:x.scholarship_name,type:"Scholarships",detail:x.status,updated_at:x.updated_at,href:"#applications"})),...(essays.data??[]).map(x=>({id:`essay-${x.id}`,title:x.title,type:"Writing",detail:x.stage,updated_at:x.updated_at,href:"#essays"})),...(resumes.data??[]).map(x=>({id:`resume-${x.id}`,title:x.title,type:"Resume",detail:"Resume version",updated_at:x.updated_at,href:"#resume"})),...(steps.data??[]).map(x=>({id:`step-${x.id}`,title:x.title,type:"Planning",detail:x.completed?"Completed":x.category,updated_at:x.updated_at,href:"#path"})),...(contacts.data??[]).map(x=>({id:`contact-${x.id}`,title:x.name,type:"Contacts",detail:x.role,updated_at:x.updated_at,href:"#support-network"})),...(decisions.data??[]).map(x=>({id:`decision-${x.id}`,title:x.school_name,type:"Decisions",detail:"Personal fit notes",updated_at:x.updated_at,href:"#college-decisions"})),...(tests.data??[]).map(x=>({id:`test-${x.id}`,title:`${x.exam} practice score`,type:"Testing",detail:String(x.total_score),updated_at:x.created_at,href:"#testing"}))];setItems(all.filter(x=>x.updated_at).sort((a,b)=>b.updated_at.localeCompare(a.updated_at)).slice(0,40))})},[userId]);
const types=["All",...Array.from(new Set(items.map(x=>x.type)))],visible=useMemo(()=>items.filter(x=>filter==="All"||x.type===filter).slice(0,expanded?40:8),[items,filter,expanded]);const relative=(date:string)=>{const days=Math.floor((Date.now()-new Date(date).getTime())/86400000);return days<=0?"Today":days===1?"Yesterday":days<7?`${days} days ago`:new Date(date).toLocaleDateString()};
return <section className="activity-center" id="recent-activity"><div className="activity-heading"><div><span className="kicker dark">RECENT ACTIVITY</span><h2>Your saved work, in one timeline.</h2><p>Use this history to confirm recent changes and return to the section where the information is stored.</p></div><div className="activity-filters" aria-label="Filter recent activity">{types.map(type=><button className={filter===type?"active":""} key={type} onClick={()=>setFilter(type)}>{type}</button>)}</div></div>{visible.length?<div className="activity-list">{visible.map(item=><a href={item.href} key={item.id}><span>{item.type}</span><div><b>{item.title}</b><small>{item.detail}</small></div><time dateTime={item.updated_at}>{relative(item.updated_at)}</time></a>)}</div>:<div className="activity-empty">Saved work will appear here as students use the trackers and planning centers.</div>}{items.filter(x=>filter==="All"||x.type===filter).length>8&&<button className="activity-more" onClick={()=>setExpanded(!expanded)}>{expanded?"Show less":"Show more activity"}</button>}<small className="activity-note">This timeline confirms that information was saved; it is not an official submission receipt. Always confirm submissions with the receiving organization.</small></section>}
